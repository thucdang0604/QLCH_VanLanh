# Danh sách nhiệm vụ: Nâng cấp Xem trước Ảnh Kích thước lớn

- `[x]` Sửa lỗi cú pháp và bổ sung các cột còn thiếu trong bảng danh sách ảnh cục bộ
    - `[x]` Khép kín thẻ `<td>` và `<tr>` cho cột Tên file thô
    - `[x]` Thêm cột Trạng thái với các badge trực quan sinh động
    - `[x]` Thêm cột Đường dẫn URL thật chứa nút Upload & Lấy URL hoặc link đã upload kèm nút copy
    - `[x]` Khép kín các thẻ đóng của bảng (`</tbody>`, `</table>`, `div`)
- `[x]` Thiết kế và triển khai cột bên phải hiển thị ảnh xem trước kích thước lớn (Large Preview Card)
    - `[x]` Thêm khung hiển thị ảnh lớn (hỗ trợ nền caro, aspect ratio tối ưu, shadow và bo tròn góc)
    - `[x]` Hiển thị thông số chi tiết: Tên tệp đầy đủ, Dung lượng tệp tinh tế (KB/MB), Định dạng
    - `[x]` Thêm các nút hành động nhanh: Copy tên file, Copy URL, Mở ảnh gốc, Upload ảnh
    - `[x]` Thêm giao diện trạng thái trống (Empty Placeholder)
- `[ ]` Kiểm tra biên dịch và xác minh hoạt động thực tế trên giao diện
